<h1>Wines for Sale</h1>
<%= page_entries_info @wines %>
<ol>
<% @wines.each do |wine| %>
  <li><%= link_to(wine.name, wine) %> is a <%= wine.varietal %> from <%= wine.winery %></li>
<% end %>
</ol>
<%= paginate @wines %>